// filepath: /c:/Users/Pc/Documents/Testes lo/automacao-api-cypress/cypress/plugins/index.js
// Remova ou comente a linha que importa e usa o cypress-cucumber-preprocessor
// const cucumber = require('cypress-cucumber-preprocessor').default;

module.exports = (on, config) => {
  // Remova ou comente a linha que configura o preprocessor
  // on('file:preprocessor', cucumber());
};